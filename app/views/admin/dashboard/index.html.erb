<div class="container">
  <%= render(Shared::PageTitleComponent.new(title: t(".title"))) %>

  <div>Add dashboard metrics</div>

  <div class="mt-4">
    <h2 class="h5 fw-bold"><%= t ".resource_title" %></h2>
  </div>

  <%= render partial: "resource_card", locals: {
        title: t("admin.dashboard.resource_card.account_title"),
          subtitle: t("admin.dashboard.resource_card.subtitle"),
          resource_index: admin_users_path, resource_new: new_admin_user_path
      } %>

  <%= render partial: "resource_card", locals: {
        title: t("admin.dashboard.resource_card.inventory_title"),
          subtitle: t("admin.dashboard.resource_card.inventory_subtitle"),
          resource_index: admin_products_path, resource_new: new_admin_product_path
      } %>

</div>
