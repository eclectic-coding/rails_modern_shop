<% if !product_available? %>
  <span class="badge bg-info">Archived</span>
<% elsif product_quantity.positive? %>
  <span class="badge bg-success">Active</span>
<% elsif product_quantity.zero? %>
  <span class="badge bg-danger">Out of Stock</span>
<% end %>